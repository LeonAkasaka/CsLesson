using System;

namespace PrimeNumber
{
    public static class IntegerExtensions
    {
        /// <summary>
        /// 整数が素数かどうかを取得する。
        /// </summary>
        /// <param name="value">調べる値。</param>
        /// <returns>素数なら true。そうでなければ false。</returns>
        public static bool IsPrime(this uint value)
        {
            throw new NotImplementedException();
        }
    }
}
